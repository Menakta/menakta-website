import type { Metadata } from "next";
import { Bebas_Neue, Space_Grotesk, Oswald} from "next/font/google";
import Navbar from "./(components)/Navbar";
import Footer from "./(components)/Footer";
import "./globals.css";

// Alternative to Tusker - bold display font
const bebasNeue = Bebas_Neue({
  weight: "400",
  variable: "--font-tusker",
  subsets: ["latin"],
});


// Alternative to ClashDisplay - modern geometric sans
const spaceGrotesk = Space_Grotesk({
  variable: "--font-clash",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Menakta Tech",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${bebasNeue.variable} ${spaceGrotesk.variable} antialiased bg-black`}
      >
        <Navbar/>
        {children}
        <Footer/>
      </body>
    </html>
  );
}
